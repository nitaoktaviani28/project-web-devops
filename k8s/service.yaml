apiVersion: v1
kind: Service
metadata:
  name: devops-demo-app
  namespace: default
  labels:
    app: devops-demo-app
spec:
  type: NodePort
  selector:
    app: devops-demo-app
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
      protocol: TCP
